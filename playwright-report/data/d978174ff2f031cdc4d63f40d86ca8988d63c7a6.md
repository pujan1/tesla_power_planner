# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: site-planner-workflow.spec.ts >> Tesla Energy Site Planner Workflow >> should allow a user to sign up, log in, and design a site
- Location: e2e/tests/site-planner-workflow.spec.ts:10:7

# Error details

```
Error: expect(locator).toBeVisible() failed

Locator: getByText('User created successfully')
Expected: visible
Error: element(s) not found

Call log:
  - Expect "toBeVisible" with timeout 5000ms
  - waiting for getByText('User created successfully')

```

# Test source

```ts
  1  | import { test, expect } from '@playwright/test';
  2  | 
  3  | test.describe('Tesla Energy Site Planner Workflow', () => {
  4  |   const testUser = {
  5  |     username: `testuser_${Date.now()}`,
  6  |     password: 'Password123!',
  7  |     name: 'Test Engineer'
  8  |   };
  9  | 
  10 |   test('should allow a user to sign up, log in, and design a site', async ({ page }) => {
  11 |     // 1. Navigate to landing page
  12 |     await page.goto('/');
  13 |     
  14 |     // 2. Sign Up
  15 |     await page.getByText('Create an account').click();
  16 |     await page.getByPlaceholder('Full Name').fill(testUser.name);
  17 |     await page.getByPlaceholder('Username').fill(testUser.username);
  18 |     await page.getByPlaceholder('Password').fill(testUser.password);
  19 |     await page.getByRole('button', { name: 'Sign Up' }).click();
  20 |     
  21 |     // 3. Log In
> 22 |     await expect(page.getByText('User created successfully')).toBeVisible();
     |                                                               ^ Error: expect(locator).toBeVisible() failed
  23 |     await page.getByPlaceholder('Username').fill(testUser.username);
  24 |     await page.getByPlaceholder('Password').fill(testUser.password);
  25 |     await page.getByRole('button', { name: 'Login' }).click();
  26 | 
  27 |     // 4. Verify Dashboard Welcome
  28 |     await expect(page.locator('h2')).toContainText(testUser.name);
  29 | 
  30 |     // 5. Interact with Site Planner Sidebar
  31 |     // Add 2 Megapack XLs
  32 |     await page.locator('div:has-text("Megapack XL")').locator('input[type="number"]').fill('2');
  33 |     
  34 |     // 6. Verify Statistics Update
  35 |     await expect(page.getByText('$250,000')).toBeVisible();
  36 |     await expect(page.getByText('8 MWh')).toBeVisible();
  37 | 
  38 |     // 7. Toggle to 3D View
  39 |     await page.getByText('3D VIEW').click();
  40 |     await expect(page.locator('canvas')).toBeVisible();
  41 | 
  42 |     // 8. Save Layout
  43 |     await page.getByRole('button', { name: 'Save Layout' }).click();
  44 |     
  45 |     // 9. Verify Success Alert
  46 |     page.on('dialog', async dialog => {
  47 |       expect(dialog.message()).toContain('Site layout saved successfully');
  48 |       await dialog.accept();
  49 |     });
  50 |   });
  51 | 
  52 |   test('should maintain state after page reload', async ({ page }) => {
  53 |     // Basic Login
  54 |     await page.goto('/');
  55 |     await page.getByPlaceholder('Username').fill(testUser.username);
  56 |     await page.getByPlaceholder('Password').fill(testUser.password);
  57 |     await page.getByRole('button', { name: 'Login' }).click();
  58 | 
  59 |     // Add 10 Powerpacks
  60 |     await page.locator('div:has-text("Powerpack")').locator('input[type="number"]').fill('10');
  61 |     
  62 |     // Reload
  63 |     await page.reload();
  64 | 
  65 |     // Verify it's still there
  66 |     await expect(page.locator('div:has-text("Powerpack")').locator('input[type="number"]')).toHaveValue('10');
  67 |   });
  68 | });
  69 | 
```