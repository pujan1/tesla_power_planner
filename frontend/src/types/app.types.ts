/**
 * @module app.types
 * Application-level type definitions.
 */

/** The top-level view states for the unauthenticated/authenticated router in App. */
export type ViewMode = 'LOGIN' | 'CREATE' | 'DASHBOARD';
